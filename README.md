# Tudur's Vehicle Mod (Fabric 1.21.11)

**作者**: Tuduraw
**ライセンス**: MIT License(`LICENSE`を参照)

> **生成AIの使用について**
> このプロジェクトのコードおよびドキュメントは、Anthropic社の生成AI
> 「Claude」を使用して作成されています。

MCヘリコプターやFlan's Modのような「乗り物MOD」を、**JSON定義 + OBJモデル**だけで
拡張できる形にしたひな形プロジェクトです。船舶・航空機・潜水艦・武装・スポーンアイテム・
専用クリエイティブタブに対応しているほか、MCヘリ用アドオンパックをこのプロジェクトの
形式へ変換するツール(`tools/mcheli_convert.py`)も同梱しています。

> **重要**: アドオンパックの作成にあたっては、他者が権利を有するモデル・
> データの取り扱いについて`GUIDELINES.md`を必ず確認してください。MCヘリ
> 向けに公開されている拡張パックの変換にも制限があります。

このドキュメント(`README.md`)は開発者向けのアーキテクチャ資料です。**アドオンパック
製作者向けの、設定可能な全項目の詳細な一覧**は以下を参照してください:

- `GUIDELINES.md` — 利用ガイドライン(権利の取り扱い・表現に関する注意)
- `Readme_Addon.md` — アドオンパックの配置方法・ディレクトリ構造・命名規則
- `Readme_Addon_Mod.md` — アドオンMOD(コードによる拡張)の作り方
- `Readme_Vehicle.md` / `Readme_Vehicle_○○.md` — 機体設定の全項目
- `Readme_Weapon.md` / `Readme_Weapon_○○.md` — 武器設定の全項目
- `Readme_HUD.txt` — HUDスクリプトの書式
- `Readme_cfg.md` — Config設定項目の一覧
- `GUIDE.md` — 上記の簡易まとめ・MCヘリアドオン変換ツールの使い方

## アーキテクチャ

```
data/<namespace>/vehicles/*.json    ← 乗り物の「データ」(サーバーでも読める。当たり判定/速度/座席/武装/使うモデル)
assets/<namespace>/models/obj/*.obj ← 見た目のメッシュ(クライアントのみ)
assets/<namespace>/textures/vehicle/*.png ← テクスチャ

VehicleDefinition (record, Codec)   ← 上記JSONをパースしたデータオブジェクト
VehicleRegistry                     ← 今読み込まれているVehicleDefinitionをID引きできる場所(/reload で再構築)
VehicleDefinitionReloadListener     ← data/.../vehicles/*.json を毎回スキャンしてVehicleRegistryを作り直す

MeshedEntity (interface)             ← OBJメッシュで描画される何か、という共通の顔
AbstractVehicleEntity implements it  ← 搭乗・座席配置・NBT同期・武装発射など共通処理
  ├ HelicopterEntity                 ← 自由飛行(空中)
  ├ CarEntity                        ← 地上走行+ステアリング
  ├ ShipEntity                       ← 水上(浮力は簡易実装)
  ├ SubmarineEntity                  ← 水上/潜航の2モード(キー操作のみ、視点非連動、詳細は後述)
  └ AircraftEntity                   ← 固定翼機(クォータニオン方式で姿勢管理、詳細は後述)

WeaponDefinition                     ← 乗り物に載せる武装1つ分(座席・威力・弾速・連射間隔)
VehicleProjectileEntity              ← ThrownItemEntity継承。弾の見た目はバニラのアイテム投擲描画を流用
FireWeaponPayload / ModNetworking    ← 発射キー入力をサーバーへ送るC2Sパケット
ThrottleInputPayload                 ← W/S/A/Dの入力意図をクライアントからサーバーへ明示的に同期
                                        (`PlayerEntity#forwardSpeed`/`sidewaysSpeed`はサーバー側では
                                        信頼できないため、乗り物操作全般でこちらを使用)

SpawnItemDefinition                  ← スポーンアイテム一覧表示用の表示名 + Tier(1〜5)
item.TieredVehicleSpawnerItem         ← 種類×Tierで25種類、固定アイコン(リソースパック不要)
client.VehicleSelectScreen           ← 右クリックで開く選択画面(Tier以下の乗り物を一覧表示)
network.SelectVehiclePayload         ← 選択結果をサーバーへ送信、種類・Tierを再検証してスポーン
ModItemGroups                        ← 専用クリエイティブタブ(25種類のスポーンアイテムを列挙)

RunwayDefinition                     ← 母艦自身のJSON定義に含める滑走路の矩形領域(乗り物の
                                        種類を問わず設定可能)
CarrierAircraftConfig                ← WeaponType.CARRIER武器1つ分の設定(艦載機・発艦/帰投
                                        ルート・カタパルト・弾薬変換半径など、詳細は後述)
CarrierRunwayPlatformEntity          ← 滑走路を実体化する、見えない床タイル(母艦へ毎tick追従)

item.DroneControlStickItem           ← 機体へ右クリックしてUUIDを登録するスティック(UAV/
                                        TargetDrone共通)
block.StationBlockEntity             ← UAVの遠隔操縦ステーション(登録スティックを挿入して紐付け)
block.DroneCenterBlockEntity         ← TargetDroneの自律AI飛行制御ブロック(円軌道/ウェイポイント
                                        巡回、Home Pointへの自動着陸)
item.DroneRouteBookItem              ← 実地飛行しながらウェイポイントを記録するアイテム
CasStrikeConfig                      ← WeaponType.CAS武器1つ分の設定(支援機・攻撃ルート)

ObjModel / ObjModelLoader (client)   ← .obj を三角形リストへパース、リロード時にキャッシュ("o"/"g"どちらのグループ宣言にも対応)
VehicleEntityRenderer (client)       ← 1クラスで全乗り物を描画。半透明テクスチャに対応(entityTranslucent)

tools/mcheli_convert.py              ← MCヘリ用アドオンパックをこのプロジェクトの形式へ変換するツール(詳細は後述)
```

### なぜ「乗り物ごとに新しいItem/EntityTypeを作らない」のか

バニラの仕組み上、**ItemやEntityTypeなどのレジストリはMod初期化時(ワールド/データパック読み込みより前)に
確定してしまう**ため、データパックで後から追加した乗り物のために新しいItemクラスを動的に登録することはできません。
そのためこのMODは:

- **EntityTypeは「操縦方法の種類」ごとに1つ**(ヘリ・車・船・潜水艦・航空機)。見た目が違う乗り物は
  同じEntityTypeに別の`VehicleDefinition`を割り当てるだけ。
- **Itemも「種類×Tier」の25種類だけ**(車Tier1〜5、船Tier1〜5、…)。どの`VehicleDefinition`を
  実際にスポーンするかは、アイテムを使った時に開く選択画面でプレイヤー自身が選ぶ。

という設計にしています。

### 新しい乗り物を「コードなし」で追加する

見た目だけ違う乗り物(操縦感覚は既存のいずれかと同じでよい場合)は、コード変更ゼロで追加できます。

1. Blenderなどで `.obj` をエクスポート → `assets/tudursvehiclemod/models/obj/my_vehicle.obj`
2. テクスチャを `assets/tudursvehiclemod/textures/vehicle/my_vehicle.png`
3. 定義を `data/tudursvehiclemod/vehicles/my_vehicle.json` に追加(既存のjsonをコピペして値を変えるだけ)。
   `entity_type` には既存のいずれか(`tudursvehiclemod:car`, `helicopter`, `ship`, `submarine`, `aircraft`)を指定
4. (任意)`weapons` に武装を追加、`spawn_item`に表示名とTierを追加すれば、対応する種類の
   Tier式スポーンアイテムの選択画面に出るようになる(次項)
5. (任意)`is_uav`/`is_target_drone`(いずれもtrue/false)を設定すると、通常の搭乗が
   無効化され、無人機(UAV)・自律ドローン(TargetDrone)としてのみ運用できるようになります。
   詳細は後述の「無人機(UAV)・自律ドローン・近接航空支援(CAS)」セクションを参照してください
6. (任意、乗り物の種類を問わず設定可能)`runway`を設定すると、その乗り物へ空母
   (Carrier)の滑走路(艦載機の発艦・着艦・停泊が可能な甲板)を持たせられます。
   詳細は後述の「空母(Carrier)システム」セクションを参照してください

**操縦ロジック自体を変えたい**場合だけ、`AbstractVehicleEntity` を継承した新しいJavaクラス
(`updateVehicleMovement`の実装)が必要です。

### 武装を追加する

`weapons` はリストなので、1つの乗り物に複数の武装を載せられます:

```json
"weapons": [
  {
    "seat_index": 1,
    "offset_x": 0.0, "offset_y": 0.5, "offset_z": 0.6,
    "projectile_item": "minecraft:iron_nugget",
    "damage": 4.0,
    "velocity": 3.0,
    "cooldown_ticks": 5
  }
]
```

- `seat_index`: この武装を撃てるのは、その座席番号(`seats`配列のインデックス、0始まり)に
  乗っている乗客だけ
- `projectile_item`: 弾として飛ばす**既存のアイテムID**。バニラの投げたアイテムの描画をそのまま使う
- 発射は既定でRキー(`key.tudursvehiclemod.fire_weapon`)。現状は武装インデックス0番を撃つ決め打ちの簡易実装
- サーバー側で「本当にその座席に乗っているか」「クールダウン中でないか」を再検証してから発射する

### スポーンアイテム(Tier式)

乗り物の**種類ごと**(車・船・潜水艦・航空機・ヘリコプターの5種類)に、Tier1〜5のスポーンアイテムが
最初から用意されています(計25種類、`ModItems.TIERED_SPAWNERS`)。アイコンは種類ごとに固定
(MOD本体に同梱、リソースパック不要)です。

**使い方**: 対応する種類・Tierのスポーンアイテムを持った状態で右クリックすると選択画面が開きます。
選ぶとその場でスポーンし、アイテムを1つ消費します(クリエイティブモードでは消費されません)。
乗り物を**しまう**(誰も乗っていない状態でスニーク+右クリック)と、対応するスポーンアイテムが
1つ手元に返ってきます。クリエイティブモードでは複製を防ぐためアイテムを渡さずそのまま消滅します。

- 乗り物側で設定するのは`spawn_item.tier`(1〜5、省略時1)と`display_name`だけです
- Tierごとのアイコンは外周の縁取りで視覚的に区別されます(Tier2=緑角括弧、Tier3=青全周枠、
  Tier4=紫全周枠+白点、Tier5=金太枠+白点)

### 操縦方式(スロットル式 + 視点追従)

- **W/S = スロットル**: クルーズコントロール方式。W/Sでスロットル値を上下させ、離すとその値を維持。
  実際の速度はスロットル値 × `max_speed`へ毎ティックなめらかに近づきます
- **車・船**: A/Dが直接ステアリング
- **航空機・ヘリ**: 機首の向き(ヨー+ピッチ)は操縦者の視点の向きに追従します。
  A/Dは見た目のバンク(傾き)になります
- **潜水艦**: 視点操作は一切機体に反映されません(常にフリールック相当)。詳細は後述の専用セクション参照
- **車両のしまい方**: 誰も乗っていない状態でスニーク+右クリック
- **HUD表示**: 画面下部にスロットルバーと、実際の速度(**kb/h**、1kb=1,000ブロックとして
  1時間あたりの移動ブロック数を表示)が常時表示されます

### キーバインド一覧(既定値、いずれもコントロール設定から変更可能)

| キー | 機能 |
|---|---|
| 右クリック | 武器の発射 |
| R | 座席に割り当てられた武器の切り替え |
| X | 選択中の武器のモード切り替え(`ModeNum`) |
| Y | 座席の切り替え(次の座席へ) |
| T | 座席の切り替え(前の座席へ) |
| Alt+Y | 空母(Carrier)発艦機体⇔発射母艦の座席切り替え(物理キーはYと共用、
  Alt(フリールックキー)を押しながらだと発艦機体側への切り替えとして扱われます) |
| 左Alt | フリールック(押している間は機体の向きに視点が追従せず、自由に見回せます) |
| 左Shift | 降下キー(ヘリコプター・VTOLの降下用。スニークは乗り物では降車に
  使われるため専用キーです) |
| V | VTOLのヘリコプターモード⇔航空機モードの切り替え |
| H | ハッチ・キャノピーの開閉 |
| G | ランディングギアの手動格納・展開 |
| N | 主翼の折りたたみ・展開(可変翼機は飛行中でも切り替え可能) |
| C | 複数設定されたカメラ位置(`CameraPosition`)の切り替え |
| PageUp/PageDown | 航空機の三人称視点ズーム |
| 上/下矢印キー | 潜水艦の姿勢を変えずに上昇・下降 |
| K | 機体メニュー(燃料・弾薬補給、カーゴ、武器タブ)を開く |
| J | オフハンドにDrone Route Bookを持っている間、ウェイポイント記録の開始・停止 |
| M(長押し1秒) | マニュアルモード(自動水平化の無効化)の切り替え |

### クリエイティブタブ

`ModItemGroups`が専用タブを登録します。中身は`VehicleRegistry`から動的に列挙されるため、
データパックでの乗り物の追加/削除や`/reload`にも自動的に追従します。

### OBJモデルの制約

`ObjModel`/`ObjModelLoader`は`v`/`vt`/`vn`/`f`のみ対応(n角形はファン三角形分割)。
`.mtl`/`usemtl`は無視 — テクスチャは1乗り物1枚(JSON側の`texture`フィールド)固定です。
グループ分け(`spinning_parts`/`toggle_parts`用)は`o`/`g`どちらの宣言にも対応しています。

---

## 航空機(AircraftEntity)の現在の実装

このプロジェクトの中で最も作り込まれている乗り物です。以下、現在の挙動をまとめます。

### 姿勢管理: クォータニオン方式

ヨー・ピッチ・ロールを別々のEuler角として管理するのではなく、単一の`Quaternionf orientation`
フィールドで一元管理しています。入力は「現在の姿勢に対する増分回転」として適用され
(`orientation.mul(delta).normalize()`)、ヨー/ピッチ/ロールの数値が必要な場面(カメラ・座席・
回転パーツなど)でのみ、都度Euler角へ変換して取り出します(`extractYawPitchRoll`)。

この方式により、バニラの視点操作(±90度でクランプされる)を一切経由しない独自の視点入力
パイプライン(`PlayerLookRateMixin`でマウスの生の移動量を捕捉→`AircraftOrientationInputPayload`
でサーバーへ送信)と組み合わせて、**ループ(宙返り)が制限なく可能**になっています。

- **既知の制限**: ジンバルロックそのものは完全には解消していません(ピッチが±90度付近の
  瞬間はヨー・ロールが同じ軸として振る舞う、という数学的な性質のため)。ロール自動水平化を
  ピッチが浅い時ほど速く効かせることで、発生頻度は減らしています

### 飛行物理

- **地上/水上(surfaced)**: ヨーのみ視点に追従、ピッチ・ロールは`OnGroundPitch`(後述)へ
  向けて固定。スロットル操作で加減速しますが、**減速側は加速側よりゆっくり効きます**
  (以前の設定の半分の速さ。加速や空中での挙動には影響しません)
- **通常飛行(スロットル50%以上)**: ヨー・ピッチとも視点に追従、A/Dでロール(360度バレルロール可)。
  速度はスロットル×`max_speed`へなめらかに追従
- **滑空(スロットル50%未満、空中)**: 速度をゆっくり失いながら滑空し、失った速度に応じて
  重力が段階的に強まる(完全に速度を失うと通常の落下と同じ重力の6倍)、失速のような挙動
- **着陸装置(ギア)展開中**: 最高速度が70%に低下(空気抵抗を表現)
- **減速(空中飛行時、スロットルを下げた時)**: 加速時より1.5倍速く効きます
  (地上でのブレーキは上記の通り別途ゆっくりめに設定されています)

### 無人時(パイロットがいない場合)の挙動

- 有人時と**同じ物理演算**(地上/通常飛行/滑空の判定・処理)を通ります。専用の別処理はありません
- スロットルが毎ティック少しずつ弱まっていきます(0.01/tick、フルスロットルから0まで約5秒)
- 姿勢は、猶予期間なしで常に水平飛行(現在の進行方向のまま、ピッチ0・ロール0)へ緩やかに近づきます
- クライアント側では独自に位置・速度を計算し直さず、サーバー側の計算結果(同期される速度)を
  そのまま使って`move()`するだけにしてあります。これはサーバー・クライアントそれぞれが独立に
  計算すると、値がズレて同期のたびに「補正されてカクつく」ため

### `OnGroundPitch`(地上姿勢)

MCヘリの`OnGroundPitch`と同様の機能を実装しています。地上/水上にいて、かつ
**十分に減速している(最高速度の15%以下)**間、指定した角度(度)へ向けてなめらかに
ピッチを合わせます(有人・無人問わず動作)。
着陸直後、まだ速度が残っているロールアウト中は、着地した瞬間の姿勢をそのまま保ち、
減速し切ってから地上姿勢へ移行します。目標のヨーは、この移行が
始まった瞬間の値を一度だけ固定して使います(毎ティック再計算すると、
SLERP(球面線形補間)の性質上ヨーが意図せずドリフトし、不安定になるため)。

逆に、離陸のため加速中で**最高速度の15%以上**(地上姿勢のしきい値と統一)に達すると、
機体を水平(ピッチ・ロールとも0)へ姿勢を合わせます。地上姿勢(尾輪式などで機首上げの
ことが多い)のまま滑走を続けるのではなく、離陸に向けて姿勢を整える形です。

地上での加速性能は、この姿勢制御が離陸前に間に合うよう、通常の半分の速さにしています。

**姿勢の同期について**: ヨー・ピッチ・ロールは、バニラ標準の同期の仕組み(`super.setYaw`/
`getYaw`など)に頼らず、専用の同期フィールド(DataTracker)で明示的に同期しています。
調査の結果、バニラ標準の同期は、**位置が変化していない間は回転の同期を送らない**という
最適化を行っているらしく、地上で静止した機体のピッチが着陸時の姿勢のまま固まって見える、
という不具合の原因になっていました。スロットル・ギア状態などの同期に既に使っている
DataTrackerの仕組みは位置の変化と無関係に同期されるため、この問題を避けられます。

### 失速

飛行中(非接地時)、実際の速度が最高速度の20%を下回ると失速状態になり、パイロットの
操作や無人時の水平飛行補正よりも優先して機首を強制的に真下へ向けます。最高速度の
30%まで回復すると解除されます(発動20%・解除30%とヒステリシス(発動と解除の
しきい値を変える仕組み)を持たせることで、しきい値付近での頻繁な切り替わりを防止)。

### 急降下加速・上昇時の減速

- 上昇(機首上げ)・降下(機首下げ)は単一の連続式で処理し、ピッチ0度を境に
  ブランチが切り替わる不具合(降下45度付近での速度低下、上昇減速が効かない
  ケース)を解消
- **降下**: 機首下向き角度に連動して連続的に速度目標が上がる(垂直降下で
  最高速度の1.5倍)
- **上昇**: 機首上向き角度に連動して連続的に速度目標が下がる(垂直上昇で0)
- **離陸可能・滑空状態の判定**: 実際の速度が最高速度の何%かで判定

### マニュアルモード

トリガーキー(既定M、長押し1秒でトグル)で、水平飛行への自動補正(無操作
5秒後の自動水平化)を完全に無効化できます。HUDに状態表示。

### 速度表示

HUDに実際の速度をkb/h(1kb=1,000ブロック)で表示。

### HUD表示

デフォルトでは、スロットルバー・速度(kb/h)・FREE LOOK/MANUAL MODE表示のみの
簡易HUDです。

**MC Heli互換のHUDスクリプト機能**に対応しています。MC Heliの`Readme_HUD.txt`
(HUD設定ファイル仕様)に準拠したテキスト形式のHUDスクリプトを、コード変更なしで
そのまま(またはほぼそのまま)読み込んで描画できます。

- 配置場所: `assets/<namespace>/hud/<name>.txt`(このMODの名前空間に限らず、
  リソースパックや他MODの名前空間からも読み込みます)
- 参照するテクスチャ: `assets/tudursvehiclemod/textures/gui/<name>.png`(256x256推奨、
  MC Heli本家と同じ)に配置してください
- 乗り物のJSON定義ファイルに`"hud": "<スクリプト名>"`を追加すると、そのスクリプトが
  使用されます(MC Heli本家と同じく、機体ごとの設定でHUDファイルを指定する方式)。
  未指定、またはスクリプトが見つからない場合は簡易HUDにフォールバックします
  (`aircraft.json`には`"hud": "plane"`、`helicopter.json`には`"hud": "heli"`を
  既定で設定済み)
- テクスチャは、スクリプトと同じ名前空間に限らず、読み込まれている全ての
  名前空間から名前(拡張子なし)で検索します(例: 移植データが`mcheli`名前空間の
  ままでも動作します)
- 対応済み: `Call`/`Exit`/`If`~`EndIf`(入れ子非対応、本家同様)/`Color`/
  `DrawString`/`DrawCenteredString`/`DrawTexture`/`DrawRect`/`DrawLine`/
  `DrawLineStipple`(破線は簡略化し実線描画)/`DrawGraduationYaw`/
  `DrawGraduationPitch1`/`DrawGraduationPitch2`/`DrawCameraRot`。式(四則演算・
  比較・論理演算・三項演算子・16進数)や変数参照にも対応
- 未対応: `DrawEntityRadar`/`DrawEnemyRadar`(このMODにレーダー装備の概念が
  ないため、読み込みはするが描画は何もしない扱い)
- 変数: 本家の変数一覧(`yaw`/`pitch`/`roll`/`throttle`/`pos_x`など)にほぼ
  対応。このMODに該当概念がないもの(HP・燃料・武器・レーダー等)は安全な
  既定値(HPは常に満タン、燃料は常に満タン等)を返します。独自拡張として
  `manual_mode`(マニュアルモード中か)・`stalling`(失速中か)・
  `gear_deployed`(ギア展開中か)・`speed`(実速度、ブロック/tick)・
  `speed_kbh`(同、kb/h)変数も追加しています
- `stick_x`/`stick_y`: 航空機はロール入力(A/D、保持状態なので安定)と
  マウスのピッチ入力(保持状態ではなく毎ティック消費されるため、値が
  瞬間的になります)。それ以外の乗り物は横方向・前後方向の入力を使用します
- テクスチャは実際のPNGファイルの寸法を読み取って正しくUV座標を計算します
- リソースリロード(F3+T等)で再読み込みされます

### ランディングギア

- **自動展開**: 着陸した瞬間に自動展開します。自動収納はしません(手動のみ)
- **手動格納/展開**: Gキー(既定、`key.tudursvehiclemod.gear_toggle`)。次に実際に着陸すると
  自動展開が上書きします
- 展開中は最高速度が低下します(前述)

### ハッチ・キャノピー

Hキー(既定、`key.tudursvehiclemod.hatch_toggle`)で開閉するトグルパーツに対応しています
(回転式・スライド式の両方)。1回のキー操作でハッチ・キャノピーの両方が同じ状態(開/閉)
に切り替わります。

- 召喚直後は、ハッチは閉じた状態です(キャノピーは従来通り開いた状態)
- 搭乗時、ハッチは搭乗前の状態を維持します(自動では開閉しません)。キャノピーは従来通り、
  搭乗時に自動で格納(閉)されます

### 三人称視点でのプレイヤーモデル連動

搭乗中のプレイヤーモデル(三人称視点で見える自分の姿、または他プレイヤーから見た搭乗者の姿)が、
**機体のパーツの一部として**機体の姿勢に完全に連動します(体全体が機体のヨー・ピッチ・ロールと
一致)。

**頭のヨー(左右)方向は、フリールック中でない限り機体の向きに固定されます。ピッチ
(上下)方向は現在もバニラ本来の挙動(プレイヤー自身の視点方向に応じて独立して動く)
のままです。**

実装は`LivingEntityRenderer`へのMixin3点(`AircraftRidingRenderState`インターフェース、
`LivingEntityRenderStateMixin`、`LivingEntityRendererMixin`)です。`setupTransforms`メソッド
(バニラがボディヨー回転を行列スタックへ適用する専用メソッド)の`bodyYaw`引数自体を機体の
ヨー角へ差し替え、その直後にピッチ・ロールぶんの回転を追加で乗算しています。

### カメラ

- 通常時: 機体の向きに完全に追従するカメラ(コックピット視点、傾きも反映)
- フリールック中: プレイヤー自身の視点操作がそのまま反映され、機体の向きには影響されません
  (三人称の場合、バニラ自身の現在のカメラ回転に沿ってズーム距離ぶん引いた位置になります)
- 三人称ズーム: PgUp/PgDn(既定)で4ブロック単位、地形との衝突判定つき
- 三人称の対面視点(F5を2回押す、バニラの2段階目の三人称視点)にも対応しています

---

## 潜水艦(SubmarineEntity)の現在の実装

マウス操作を一切機体操作に反映しない、完全キー操作の乗り物です。視点は常に
(航空機のフリールックと同様に)自由に動かせます。

### 水上/潜航の切り替え

Hキー(既定、ハッチ/キャノピー用のキーと共用)で切り替えます。この状態は
ハッチ系トグルパーツ(`toggle_parts`の`trigger: "key"`)の開閉状態と共通で、
潜航に入るとハッチが閉じ、浮上するとハッチが開きます。スポーン直後は水上航行
モード(ハッチ開)です。

### 水上航行モード

艦船(ShipEntity)と全く同じ操縦感覚(スロットル+A/Dによる直接ヨー旋回)ですが、
以下の2点が異なります。

- **旋回時の傾き**: 艦船と逆になります。艦船は重心が水面より上にあるため遠心力で
  外向きに傾きますが、潜水艦は重心が水面より下にあるため内向きに傾きます
- **浮力**: 艦船の離散的な(潜水中/水面接触中/水面から離れている、の3段階で
  重力値を切り替える)方式ではなく、周囲の水面の高さを都度探索し、その少し下を
  目標にゆるやかに近づける連続的なばね式の補正に変更しています。艦船と同じ
  離散方式では水面付近で常時浮き沈みを繰り返す不安定な挙動が発生したため

潜航中は`AddPartWeapon`等の武器パーツの照準連動が停止し(向きが固定されます)、
武器の発射自体もできなくなります。潜航中も使用できるのは魚雷(`Type=Torpedo`)
のみですが、武器設定ファイルに`UsableWhileDiving = true`を指定すれば、他の
武器タイプも潜航中に使用可能にできます。使用不可な武器を選択している間は、
`weapon_bay`トリガーの武器倉パーツも(選択されていても)開きません。

### 潜航モード

- **W/S**: スロットル(水上航行モードと共通、後述の制限あり)
- **A/D**: 進路変更(水上航行モードと共通、直接ヨー旋回)
- **Space**: 機首上げ(浮上方向にピッチ変化)
- **左Shift(既定、降下キーと共用)**: 機首下げ(沈降方向にピッチ変化)
- **十字キー上**: 姿勢を変えずにそのまま上昇
- **十字キー下**: 姿勢を変えずにそのまま下降

航空機と同様、無操作が一定時間(5秒)続くとピッチが水平(0度)へ自動的に
戻ります。マニュアルモード(Mキー長押し、航空機と共通)でこの自動水平化を
無効化できます。潜航中はロールは常に0へ戻ります(水中でのバンクはありません)。

機首上げ状態のまま水面に到達すると、航空機の失速よりも緩やかに機首が
水平方向へ戻り、その間はやや強めの下向きの力が加わります(浮上し続けて
しまうのを防ぐため)。

水中にいる間、操作(スロットル・ピッチ・十字キー)以外の要因では一切
上下移動しません(重力・浮力は完全に無効化しています)。水から完全に
出た場合(座礁など)のみ、通常の重力が働きます。

### スロットルの制限

前進は100%まで、後進は-20%までに制限されています。プラスとマイナスを
直接切り替えることはできず、0%の状態で2秒間待機してから逆方向に
加速し始めます。

### 無人時の挙動

パイロットがいない場合も、スロットルが自然と0%へ戻っていく点を除いて
有人時と全く同じ処理を通ります(専用の別処理はありません)。

### カメラ・プレイヤー表示

視点(ヨー・ピッチ)は常にプレイヤー自身の操作がそのまま反映されます
(フリールック相当)が、ロール(傾き)だけは機体自身のものがカメラ・
三人称プレイヤー表示の両方に反映されます(航空機のフリールック時と同じ
仕組みを共有しています)。

---

## 無人機(UAV)・自律ドローン・近接航空支援(CAS)

プレイヤーが直接搭乗しない形で乗り物を運用する、3つの独立した仕組みです。

### UAV(無人航空機、遠隔操縦ステーション)

機体JSON定義に`"is_uav": true`を設定すると、その機体は通常の搭乗(座席への直接乗車)が
できなくなります。代わりに以下の手順で遠隔操縦します:

1. **Drone Control Stick(登録スティック)**を持って対象の機体に右クリックすると、その
   スティックへ機体のUUIDが登録されます(1本のスティックは常に直近登録した1機のみを
   保持し、再登録すると上書きされます)
2. **UAV Station(ステーションブロック)**へ登録済みスティックを挿入すると、そのステー
   ションがその機体に紐付けられます
3. ステーションを右クリックすると、プレイヤーはその機体へ遠隔操縦として搭乗します。
   視点入力・武器発射も含め、通常の搭乗と全く同じ操作感覚で操縦できます
4. 機体が読み込み範囲外にある場合、ステーションは最後に確認した座標のチャンクを一時的
   に強制読み込みし(最大5秒)、機体を探索します

### 標的ドローン(TargetDrone、自律AI飛行)

機体JSON定義に`"is_target_drone": true`を設定すると、同様に通常の搭乗ができなくなり
ます。UAVと全く同じ登録スティックの仕組みを使い、**Drone Center(ブロック)**へスティ
ックを挿入して紐付ける点も共通ですが、Drone Centerが有効化している間、機体はプレイ
ヤーの操作なしに自律AIで飛行します。

- **円軌道モード(デフォルト)**: ウェイポイントが未設定の場合、Drone Centerの上空を
  指定高度で円軌道飛行します。旋回半径は機体自身の性能(旋回速度・巡航速度)から自然に
  導出されます(固定値ではありません)
- **ウェイポイント巡回モード**: Drone Centerの設定画面から複数のウェイポイントを直接
  入力するか、**Drone Route Book**(アイテム)を挿入することで、そのルートを巡回飛行
  します
- **Drone Route Book**: オフハンドに持って実際に機体で飛行しながらJキーでウェイポイ
  ントの記録を開始・終了できる、実地飛行によるルート記録用アイテムです
- **Home Point**: 着陸目標地点です。デフォルトはスティック紐付け時の機体位置で、他の
  ウェイポイントと同様に編集可能です
- 設定画面から有効化・無効化を切り替えられます。飛行中に無効化すると、その場で操作を
  失うのではなく、Home Point付近へ安全に着陸してから停止します
- Drone Centerも、機体が読み込み範囲外に出ないよう、機体の現在位置を中心とした3x3
  チャンクを常時強制読み込みします

### 近接航空支援(CAS、`WeaponType.CAS`)

母艦・地上車両等に搭載する武器の設定ファイル(`.txt`)で武器種別を`CAS`にすると、その
武器は実弾を発射する代わりに、支援機(別の乗り物定義)をその場でスポーンさせ、設定され
たウェイポイントルートを自動飛行させながら攻撃させます。

- 発射すると、クロスヘアの照準先(地上の目標地点)がルートの基準座標としてマークされ
  ます
- 設定された支援機がルートの最初のウェイポイント位置にスポーンし、ルートを自動飛行し
  ます
- `attack=true`が設定されたウェイポイントを通過している間、指定した武器スロットで
  自動的に射撃します
- 最後のウェイポイントに到達すると消滅します(空母(Carrier)の艦載機とは異なり、帰投・
  着陸はしません)
- 生存期間のタイムアウト・一定時間進行がないまま停滞した場合のタイムアウトという、
  2種類の安全装置を持ちます

これらの仕組みは、後述の空母(Carrier)システムとも共通の基盤(ウェイポイント巡回・
攻撃判定・チャンク強制読み込み)を土台としています。

---

## 空母(Carrier)システム

乗り物を空母として機能させ、艦載機の発艦・自動飛行・帰投を管理する機能です。
**特定の乗り物タイプに限定した機能ではありません。** 実装上も`entity_type`への
依存は一切なく、艦船に限らずどの乗り物にも設定できます。「空中空母」のような
架空の航空機に艦載機を発艦させることも、以下の項目だけで実現できます。

### 滑走路(`runway`/`runways`)

**滑走路(`runways`)と、後述する艦載機発艦武器(`Type = CARRIER`)は互いに必須の
関係ではなく、それぞれ単独でも利用できます。** 武器を持たず甲板としてのみ滑走路を
設定することも、逆に`Type = CARRIER`の武器だけを設定し滑走路は定義しない(発艦
した機体を別の手段で回収する前提とする)こともできます。両方を組み合わせて
「発艦させた艦載機が実際にこの滑走路へ着艦して回収される」という一連の動作にする
ことも可能で、これは空母の基本形として元々意図されている組み合わせです(詳細は
後述の「艦載機発艦武器との組み合わせ」を参照)。

母艦となる乗り物自身のJSON定義に`runway`オブジェクト(1つのみ)、または`runways`
配列(複数の甲板を持つ場合、任意個数)を追加すると、その乗り物の甲板上に滑走路が
定義されます。

```json
"runway": {
  "width": 30.0,
  "center_x": 0.0,
  "height_y": 12.0,
  "start_z": -105.0,
  "end_z": 90.0
}
```

- 座標は全てこの母艦自身のエンティティ位置からの相対値で、母艦の現在の向き(ヨー)に
  合わせて回転します。`center_x`を中心に`width`ブロック幅、`start_z`〜`end_z`
  (順不同)の範囲、母艦位置から`height_y`ブロック上の高さに、矩形の滑走路が定義されます
- 実体としては、`CarrierRunwayPlatformEntity`(見えない床タイル)の集合が滑走路の
  範囲全体に敷き詰められ、艦載機の着陸装置・失速判定・当たり判定を、実際にブロックが
  そこにあるかのように扱います。タイルのサイズは`carrierRunwayExpectedWidth`
  (サーバー設定、既定30.0)から自動的に算出されます
- 滑走路が定義されていない乗り物は、この機能を一切使いません(通常の乗り物として動作)
- 複数の甲板・エレベーター等、独立した滑走路を複数持たせたい場合は`runways`配列を使います:
  ```json
  "runways": [
    { "width": 30.0, "center_x": 0.0, "height_y": 12.0, "start_z": -105.0, "end_z": 90.0 },
    { "width": 5.0, "center_x": 0.0, "height_y": 2.0, "start_z": -3.0, "end_z": 3.0, "hatch_offset_y": -8.0 }
  ]
  ```

#### ハッチ連動runway(任意項目)

`runway`/`runways`の各要素には、特定の`toggle_parts`の名前には一切依存しない、以下の
任意項目を追加できます。見た目のハッチアニメーションと連動させたい場合は、addonパック側で
両方の数値を揃えてください(自動連動はしません)。

- `hatch_gated`(bool、既定`false`): `true`の場合、このrunway(とその滑走路タイル)は
  ハッチが開いている間だけ存在します。回転タイプのハッチ(揚陸艇の船首道板など、開閉の
  途中経過を追跡できないもの)向け。単純なon/off切り替えで、途中経過の考慮は一切ありません
- `hatch_offset_x`/`hatch_offset_y`/`hatch_offset_z`(double、既定`0.0`): 非ゼロの場合、
  ハッチが開くにつれてこのrunwayの実効位置がこの値ぶんまでイージングしながらオフセット
  されます(閉じると元の位置に戻ります)。上下・前後・左右の平行移動タイプのハッチ
  (空母のエレベーター甲板など)向け
- `hatch_move_speed`(double、既定`1.0`): 上記オフセットのイージング速度(進捗/秒)。
  `toggle_parts`の`"$hatch"`プレフィックス付きパーツと同じ倍率補正(設定speedの2倍で動作)
  が自動的にかかるため、見た目のハッチと同じ速度にしたい場合は、対応する`toggle_parts`の
  `speed`と同じ数値を指定してください

`hatch_gated`と`hatch_offset_*`は同時に指定することも技術的には可能ですが、通常はどちらか
一方のみの使用を想定しています。

### 艦載機を発艦させる武器(`WeaponType.CARRIER`)

母艦に搭載する武器の設定ファイル(`.txt`)で武器種別を`CARRIER`にすると、その武器は
実弾を発射する代わりに、艦載機(別の乗り物定義)をその場でスポーンさせ、設定された
ルートを自動飛行させます。**この武器は滑走路(`runways`)の有無に関わらず動作します**
(帰投した艦載機の回収は、下記`CarrierLandingToAmmoRadius`等による位置判定のみで
行われ、実際に滑走路タイルへ着地しているかどうかは問いません)。主な設定項目:

- `CarrierAircraft`: 発艦させる艦載機の乗り物ファイル名
- `CarrierWaypoint`(繰り返し): 通常の哨戒/攻撃ルート(CAS武器と同じ書式:
  相対X,Y,Z,速度%,攻撃するか)
- `CarrierLaunchWaypoint`(繰り返し): 発艦直後に先行して飛ぶ、専用の離陸ルート。
  書式は`相対X,Y,Z,速度%,gear,bay,速度補正(km/h)`(gear/bay/速度補正の3列は
  省略可・`-`で「変更なし」)。**速度補正**は、そのウェイポイントに到達した瞬間、
  機体の水平速度の大きさをこの値(km/h)へ瞬時に上書きするカタパルト演出用の項目
  です(向きは維持、以降は通常の速度制御に任せます)。gear/bay列は着陸脚・武器倉
  の強制開閉トリガーですが、現状は0番目のウェイポイントでのみ実際に反映されます
- `CarrierLandingWaypoint`(繰り返し、1つ以上必須): 帰投時の着艦アプローチルート。
  `CarrierLaunchWaypoint`と同じ書式(速度補正列も指定できますが、着艦処理側では
  未使用です)。最後の1点が最終進入の基準点になります
- `CarrierLandingToAmmoRadius`(既定15.0): AddWeapon位置を中心とした半径。この
  範囲内にいる艦載機(有人・無人問わず)を弾薬1発分へ自動変換します。この回収
  ゾーンは常に有効で、滑走路の有無に関わらず機能します
- `CarrierRecoveryPoint`(繰り返し、任意): `相対X,Y,Z,半径`の書式で、上記の
  AddWeapon位置の回収ゾーンに加えて、追加の回収ゾーンを何箇所でも設定できます。
  座標はAddWeapon位置基準の相対値(母艦の向きに追従)。大型空母の複数の着艦
  スポットなどに利用できます

### 主な挙動

- **発艦**: 発射位置からスポーンし、`CarrierLaunchWaypoint`のルートを先に飛行してから、
  通常の`CarrierWaypoint`ルートへ合流します
- **座席の切り替え**: 発艦させたプレイヤーは、Alt+Y(既定)で発射母艦と発艦した艦載機の
  間で搭乗座席を切り替えられます
- **帰投・着艦**: `CarrierWaypoint`ルートの最終地点に到達すると、消滅せずに
  `CarrierLandingWaypoint`ルートを経由した自動着艦シーケンスへ移行します
- **弾薬への変換**: `CarrierLandingWaypoint`ルートを経由してAddWeapon位置付近まで
  戻ってきた艦載機、または`CarrierLandingToAmmoRadius`・`CarrierRecoveryPoint`
  のいずれかの回収ゾーン内にいる艦載機は、自動的にこの武器の弾薬(+1発)へ変換
  されます。この判定は位置・速度のみによるもので、実際に滑走路タイルへ着地して
  いるかどうかは問いません(滑走路を定義していない母艦でも同じように機能します)

### 母艦への追従

滑走路上に存在するエンティティ(滑走路タイル自身・艦載機・その他の乗り物・プレイヤー等)
は、母艦の移動・旋回に合わせて毎tick追従します。プレイヤーは`requestTeleport()`による
差分ベースの位置更新、その他のエンティティは母艦の前tickからの移動量をそのまま加算する
方式で追従します。無人の乗り物については、クライアント側で独自に物理演算(速度の再
計算)を行わず、サーバー側で計算・同期された速度に基づいて位置を更新するだけにして
あります。これは、クライアント・サーバーそれぞれが独立に物理演算をやり直すと、値が
僅かにズレて同期のたびに補正され、テレポートしたような見た目のカクつきになるためです。

---

## 燃料・補給・機体メニュー

- **燃料**: `max_fuel`(既定600)・`fuel_consumption`(既定0.5、1秒あたりの消費量、
  スロットルに応じた緩急カーブあり)を機体JSONで設定できます。燃料切れになると
  スロットルが0に固定されます(操縦自体は引き続き可能)
- **機体メニュー(Kキー)**: 搭乗中にKキーを押すと開きます
  - **燃料補給**: 携行缶(`FuelCanItem`)をスロットにセットすると、携行缶内の燃料が
    機体へ供給されます
  - **弾薬補給**: 対応するアイテムを消費して、各武器の弾薬を補充できます
  - **カーゴ(機体インベントリ)**: `inventory_size`が設定された機体で利用できます。
    45マス(5行×9列、クリエイティブインベントリと同じ)を1ページとし、それを超える
    サイズはページ送りで表示します(`VehicleMenuScreenHandler.PAGE_ROWS`)
- **携行缶と給油装置**: 鉄インゴットで携行缶(`FuelCanItem`)を作成できます。ピストン
  +かまど8個で給油装置を作成すると、燃料系アイテムを燃料へ変換して携行缶へ充填でき、
  携行缶から機体へ供給できます。クリエイティブ専用の無限燃料缶(`CreativeFuelCanItem`)
  も用意されています

---

## サウンドシステム

MCヘリの`Sound`/`SoundVolume`/`SoundPitch`/`SoundPitchRandom`/`SoundDelay`設定
(機体・武器双方)に対応した独自のサウンド読み込み・再生システムを実装しています。

- **武器**: `WeaponDefinition`の`sound`(未指定時は`<武器名>_snd`)・`sound_volume`・
  `sound_pitch`・`sound_pitch_random`・`sound_delay_ticks`。射撃ごとに、その武器の
  ワールド座標でワンショット再生されます。`sound_delay_ticks`は連射系武器向けの
  「実際に音を鳴らす間隔」で、発射レート(`cooldown_ticks`)とは別に管理されます
- **乗り物**: `VehicleDefinition`の`engine_sound`。エンジン音として継続的にループ
  再生され、スロットルに応じて音量・ピッチが変化します(アイドル〜フル)
- サウンドファイルは`assets/<namespace>/sound(s)/<名前>.ogg`(名前空間・
  フォルダ名`sound`/`sounds`のどちらも、全て検索対象)に配置します。`tudursvehiclemod-addons`
  のようなルースフォルダにも対応しています(`client.sound.AddonSoundLoader`が
  HUDシステムと同様の仕組みで直接スキャンします)
- 内部的にはMinecraft本体のサウンドシステム(`sounds.json`によるリソースパック
  登録)を経由せず、OGGファイルを直接デコードしてOpenALバッファへアップロードし、
  独自に再生を管理しています(ルースフォルダのファイルは`ResourceManager`から
  見えないため)
- 武器の発射音はサーバーからその乗り物を追跡している全プレイヤーへ専用パケットで
  通知し、各クライアントが個別に再生します

### 武器ステータス(実行時読み込み)

武器のダメージ・速度・発射間隔・重力・サウンド・弾丸モデルは、機体のJSONに
焼き込まれるのではなく、`assets/<namespace>/weapons/<weapon_name>.txt`
(MCヘリ形式そのまま)から**発射のたびに直接読み込まれます**
(`asset.WeaponStatsLoader`)。これはHUDスクリプトと同様の仕組みで、
`tudursvehiclemod-addons`のようなルースフォルダを直接スキャンします
(`data/`配下のみ見える通常のサーバーリソースマネージャーでは`assets/`配下は
見えないため)。

- バランス調整は`.txt`ファイルを直接編集し、`/reload`するだけで反映されます
  (再変換不要)
- 該当ファイルが見つからない場合は既定値(`WeaponStats.FALLBACK`)にフォール
  バックします
- `VehicleDefinition`の`weapons[]`には`weapon_name`(どの武器ファイルを見るか)
  のみが記録されます
- `DisplayName`(表示名)・`Round`(装弾数、0または未設定で無制限)・
  `ReloadTime`(リロード時間、tick単位)も読み取ります。装弾数のある武器は
  発射ごとに減り、0になると自動的にリロードを開始します(サーバー側で管理・
  同期)

### 武器の切り替え・発射キー

- 発射キーは既定で右クリック(`key.tudursvehiclemod.fire_weapon`)
- 武器切り替えキー(既定`R`、`key.tudursvehiclemod.weapon_switch`)で、搭乗している
  座席に割り当てられた武器の間で選択を切り替えます(他のキーバインドと同様、
  コントロール設定から変更可能です)
- 武器選択はクライアント側のみの概念です(サーバーは発射時に送られてきた
  武器インデックスを都度検証します)
- 同一の武器ファイルを参照する複数の`AddWeapon`は、MCヘリの仕様通り**単一の
  論理武器として1つにまとめられ**、発射位置(`offsets`)を発射のたびに
  設定順で巡回します(連装・多連装砲など)。個別に選択可能な別の武器としては
  扱いません

### HUDでの武器情報表示

MCヘリの`WPN_NAME`(武器名)・`WPN_AMMO`(現在の装弾数)・`WPN_RM_AMMO`
(装弾数上限)・`reloading`(リロード中か)に対応しています。選択中の武器の
情報が表示されます。

`WPN_NAME`のような文字列型の変数は、この式言語が本来数値(`double`)のみ
対応のため、`%s`指定子で参照された際に「変数名そのものの識別子」と一致する
場合のみ特別に文字列として解決される、専用の仕組みで実装しています
(`HudExecutionContext.stringVariables`)。

### 武器タイプ・照準・爆発(MCヘリ挙動の再現)

MCヘリの`Type`ディレクティブに対応した挙動を実装しています(段階的実装、
まずは単純なものから):

- **MachineGun1/MachineGun2**: 変換時に`MachineGun`として統合されます。
  照準追従(MachineGun2)か固定方向(MachineGun1)かは、**`Type`ではなく**
  `AddWeapon`の`MinYaw`/`MaxYaw`/`MinPitch`/`MaxPitch`が指定されているか
  どうかで決まります(指定があればプレイヤーの視点にその範囲内で追随、
  なければ`AddWeapon`自身のyaw/pitch(向き固定)を使用)。この判定は
  武器種別に関係なく共通の仕組みです(例:`Rocket`の`Hydra70`も範囲指定を
  持ちます)
- **Rocket**: MachineGunと同じ弾道(直進・重力は武器ファイル依存)。命中時の
  爆発・発火の扱いが主な違いです
- **Bomb**: 機体の姿勢(ピッチ・ロール)が水平から一定角度以内でなければ
  発射できません。発射すると機体自身の現在速度を引き継いだ状態で開始し、
  以降は武器ファイルの重力に従って落下します(固定初速は使用しません)
- **爆発・発火**: 武器ファイルの`Explosion`(威力、0で爆発なし)・
  `ExplosionBlock`(0より大きければブロックを破壊)・`Flaming`(命中時に
  発火、`Explosion`が0の場合は無効)に対応しています
- **Torpedo**: Bombと同様、機体姿勢が水平・低高度でなければ発射できません
  (低高度は直下の地面/水面までの距離で判定)。機体の速度を引き継いで
  開始し、重力に従って落下しますが、着水した瞬間から重力が無効になり、
  その時点の速度のまま直進を続けます(誘導はしません)
- **ASMissile**: 発射した瞬間の、プレイヤーの視線が地面と交わる点(何も
  無ければ視線方向の最大探索距離の点)を目標として、徐々に向きを変えながら
  飛行します(誘導中は重力の影響を受けません)
- **AAMissile/ATMissile**: 発射キーを押した瞬間、照準(視線の中心付近、
  設定角度以内)にいる対象へ自動的にロックオンし、そのまま追尾します
  (対象が動けば毎ティック方向を再計算。対象が死亡・消滅した場合はその後
  直進を継続)。AAMissileは**現在空中にいる**対象のみ、ATMissileは
  **現在地上または水面上にいる**対象のみをロックオン可能です(機体の種別
  ではなく、対象の現在の物理的な状態で判定。地面から一定高度以上離れており
  水に触れていない場合は「空中」、水中に潜っている(目の位置が水面下)場合は
  「水中」として区別され、いずれもAAMissile/ATMissileではロックできません。
  水面上に浮いている・立っている状態は引き続き「地上」扱いでATMissileの対象
  になります)。ロック(`LockTime`で設定した時間、同一対象を照準内に維持)が
  完了するまでは発射キーを押しても発射されません(MCヘリ本来の「ロック前でも
  無誘導で発射できる」仕様とは異なる、本プロジェクト独自の挙動です)
- **Missile**(本プロジェクト独自、MCヘリ原作には存在しないType名): AAMissile/
  ATMissileと同じロックオン・誘導・発射制限の挙動をしますが、空中/地上/水中の
  対象種別による絞り込みは一切行いません
- **ASWeapon**(本プロジェクト独自、MCヘリ原作には存在しないType名。
  `ASMissile`は既存のType名のため別名にしています): 対潜ミサイルです。
  AAMissile/ATMissileのようなロックオンではなく、ASMissileと同様に発射時の
  照準地点(水中の目標であれば、その水底付近の座標になります)を目標として
  誘導飛行します。目標までの距離が新規設定項目`DiveDistance`(既定10.0)
  ブロック以内になるまでは、目標の水平座標へ向けて水面のやや上を飛行し続け、
  その距離まで近づいた時点で目標そのものへ向けて潜航を開始します。着水した
  瞬間からは、魚雷(Torpedo)と全く同じ水中巡航システムに切り替わります
  (`AccelerationInWater`/`VelocityInWater`/`TargetDepth`をそのまま流用)。
  弾頭分離など実際の対潜兵器が持つ複雑な多段階動作は実装していません
- 誘導ミサイル共通のパラメータ(旋回速度・探索範囲・ロックオン角度)は
  現状固定値です。個別武器ファイルからの調整には対応していません
- `MkRocket`/`Dispenser`/`Smoke`/`Dummy`/`TargetingPod`は実装済みです。
  `CAS`(近接航空支援、支援機を自動出撃させ攻撃させる武器タイプ)は、通常の
  `AddWeapon`武器とは別の独立した仕組み(`WeaponType.CAS`)として実装済みです。
  詳細は後述の「無人機(UAV)・自律ドローン・近接航空支援(CAS)」セクションを
  参照してください

### 弾丸モデル

武器の`bullet_model`/`bullet_texture`/`bullet_scale`(MCヘリの`ModelBullet`から
自動変換)が設定されている場合、投射物はバニラのアイテム表示ではなく、実際の
OBJモデルで描画されます(未設定の場合は従来通りアイテム表示)。モデル/
テクスチャ/スケールは`DataTracker`でサーバーからクライアントへ同期されます
(サーバー側のみで設定した通常のJavaフィールドは、生成パケット経由の
クライアント側エンティティには一切伝わらないため)。

---

## 航跡(Wake Trail)エフェクト

Ship・Submarine(浮上中)・水上機(`Float = true`の航空機)・Car
(`SubmersibleDamageHeight`の範囲内で水に浸かっている場合)が水面上を移動する際、
船尾・船首(左右に分岐する2系統)に、小さな四角いタイルの集まりを表示します。
対象車種が水面を移動しているだけで自動的に発生し、追加の設定は不要です。

**実際のパーティクルシステムの代用**として、パーティクルではなく、車体モデル
自体の描画に使われている仕組みを再利用した軽量なポリゴン描画を用いています
(艦船の規模に関わらず自然に見えるようにするため)。各タイルは、隣接するタイルと
一切連結されない、完全に独立した個別の四角形として描画されます。連結された帯
(リボン)ではないため、航跡全体がひとつの個体であるかのように連動して形を変える
ことはありません。

各タイルは、**生成された瞬間の機体の状態(速度・向き)のみに基づいて、その後の
進み方が完全に固定**されます。生成後は機体側で何が起きても(減速・加速・旋回・
後退など)一切影響を受けません。船首の左右2系統のタイルは、生成時点の船体の
向きに対して**一定の角度**(実際の船舶が生み出すKelvin波の広がり角にならった
考え方)で、船速に比例した速さで直線的に外側へ広がり、最大幅に達すると動作を
停止して、消失するまでその場に留まります。速い船ほど短時間で最大幅に達し、
遅い船はゆっくりと、あるいは表示時間切れになるまでに最大幅へ到達しないまま
消えていくため、結果として遅い船の航跡は控えめな広がりにとどまります。船尾の
タイルは、左右への移動はせず、記録された位置にそのまま留まります。

旋回中は、内側(旋回の中心側)のタイルについて、目標到達距離・広がる角度の
両方が、その時点の旋回半径に応じて自動的に強まります(半径が小さいほど、より
遠くまで、より急な角度で広がります)。これは、旋回中の内側の航跡が幾何学的に
密度不足になりやすい(現実の船舶航跡でも同様の圧縮が起きる現象です)ことを
補うための調整です。

生成位置は固定のオフセットではなく、各モデルの命中判定用メッシュを水面の
高さでスライスし、実際のモデル形状から船首・船尾の交差点を動的に算出する
方式のため、モデルごとの追加設定は不要です。

以下の2項目は乗り物設定ファイルで調整できます(いずれも省略可能):

- `wake_trail_spread_distance`:船首の航跡が最終的に到達する最大の広がり幅
  (ブロック単位)。**省略した場合、船体の命中判定用メッシュから水面での
  実際の船幅を自動計測し、その値を使用します**(小型艇から全長200ブロック
  以上の大型艦船まで、手動調整なしで自然にスケールするため)。自動計測した
  値が意図と異なる場合のみ、この項目で上書きしてください
- `wake_trail_duration_ticks`(既定`300`、15秒):航跡の各点(タイル)が
  消えるまでの表示時間(tick単位)。船首・船尾共通

---

## MCヘリ変換ツール(`tools/mcheli_convert.py`)

MCヘリ用アドオンパック(`.txt`設定+`.obj`モデル)を、このプロジェクトの`VehicleDefinition`
JSON形式へ変換するツールです。

### 対応している主な機能

- 車両種別の自動判定(`AddRotor`/`AddCrawlerTrack`/`Float`+`Gravity`の強さから、
  ヘリ・車・船・潜水艦・水上機を判別)
- パーツファイル(`<機体名>_<パーツ名>.obj`)を本体の`.obj`へ統合し、`o`/`g`両方の
  グループ宣言として出力(一部の3Dツールは`g`しか認識しないため)
- **回転パーツ**(`spinning_parts`): `AddRotor`(ヘリのローター)/`AddBlade`(プロペラの
  実際の回転部分。`AddPartRotor`はVTOL機のローター筐体の傾きで、継続回転とは別物なので
  混同しないよう注意)/`AddPartRotation`。`RotorSpeed`(未指定時60)を「フルスロットル時の
  毎秒回転数」として解釈し、実際にスロットルへ連動して回転速度が変化します
- **トグルパーツ**(`toggle_parts`): ハッチ・キャノピー(`AddPartHatch`/`AddPartSlideHatch`/
  `AddPartCanopy`/`AddPartSlideCanopy`、Hキーで開閉)、ランディングギア(`AddPartLG`/
  `AddPartLGRev`/`AddPartSlideRotLG`、自動展開+手動格納)。同じ命名ファミリー内の複数の
  ディレクティブ種別(例: `AddPartLG`と`AddPartSlideRotLG`の混在)は、ファイル内の実際の
  出現順序に基づいて連番を振るため、命名の衝突が起きません
- **Y座標のオフセット**: 艦艇・水上機以外は、OBJ座標の-0.30がMinecraftの地面と一致するため、
  変換時に+0.30を加算します(メッシュ・座席・武器・各種パーツのピボット座標すべてに適用、
  スライド式パーツの移動量には非適用)
- **座席位置の補正**: MCヘリとの見た目の差を埋めるための追加調整(-0.5)
- **CameraPosition**: 搭乗時のカメラ位置をMCヘリの`CameraPosition`/`AddGunnerSeat`の
  カメラ座標から反映(複数のCameraPositionを設定した場合、専用キーで切り替え可能。MCヘリ
  自体はHキーを使うが、このMODではHキーを既にハッチ/キャノピー開閉に割り当てているため、
  別の専用キーを使用)
- **`OnGroundPitch`**: 大文字小文字を区別せず読み取ります(MCヘリ自体が大文字小文字を
  区別しないため、設定ファイル内の全項目名を小文字に正規化して比較しています)
- **HUD**: 機体設定ファイルの`HUD = <名前>`を読み取り、生成JSONの`"hud"`へ反映(未指定
  時は種別ごとの既定HUDを使用)。`assets/mcheli/hud/*.txt`・
  `assets/mcheli/textures/gui/*.png`を出力パックへ自動コピーし、コピーした**全ての**
  HUDファイルへ`manual_mode`/`stalling`/`gear_deployed`/`speed_kbh`の表示を自動追記
  します(各機体は自身のHUDまたは種別既定のHUDのいずれか1つしか使わないため、
  重複表示は実質的に問題になりません。再実行しても同一ファイルへの重複追記は
  されません)
- **サウンド**: 機体の`Sound = <名前>`(エンジン音)、武器の`Sound`/`SoundVolume`/
  `SoundPitch`/`SoundPitchRandom`/`SoundDelay`(発射音、`Sound`未指定時は
  `<武器名>_snd`)を読み取り、生成JSONへ反映。`assets/mcheli/sound/*.ogg`・
  `assets/mcheli/sounds/*.ogg`を出力パックへ自動コピーします
- **武器**: `AddWeapon`から座席・オフセット・`weapon_name`(武器ファイル名)を
  生成JSONへ反映するのみで、ダメージ・速度・発射間隔・重力・サウンド・弾丸モデル
  といった実際の数値は**焼き込みません**。`assets/mcheli/weapons/*.txt`を
  出力パックへコピーし、**実行時にそのファイルを直接読み込みます**(後述)。
  これにより、変換後にバランス調整したい場合は元の`.txt`ファイルを直接編集
  するだけで済み(`/reload`で反映)、再変換は不要です。ファイルが見つからない
  場合は既定値にフォールバックします(`WeaponStats.FALLBACK`)
- **弾丸モデル**: 武器の`ModelBullet = <名前>`を読み取り、
  `assets/mcheli/models/bullets/*.obj`・`assets/mcheli/textures/bullets/*.png`を
  `bullet_`接頭辞付きで`models/obj/`・`textures/vehicle/`へ再配置します
  (このプロジェクトのモデル/テクスチャ読み込みは固定パスのみ走査するため)

### 未対応の機能

- `AddRepellingHook`(ヘリコプターからモブを一定間隔で降ろすフック)

現状、変換後も静止した本体の一部として描画されます(パーツの
モデル自体は本体モデルへ統合され表示されますが、対応する動作・演出は
ありません)。

以下はMCヘリ本家の機能ですが、この MOD の設計上、**非互換**として意図的に
対応していません:

- `AddRack`/`RideRack`(機体を機体に乗せる「ラック」機能)、および
  `ExclusionSeat`(座席/ラック間の排他制御) — このMODでは滑走路
  (`runways`)による機体の搭載・追随機能で代替しています
- `AddParticleSplash`/`EnableSeaSurfaceParticle`(水上移動時の水しぶき) —
  このMOD独自の航跡生成機能で代替しています
- 地形追従の傾き設定(`OnGroundPitchFactor`/`OnGroundRollFactor`/
  `SetWheelPos`) — 地形に応じた傾き機能自体は実装済みですが、MCヘリ本家とは
  異なる計算方式のため、これらの設定項目そのものに互換性はありません
- ガンナーモード(`EnableGunnerMode`)、および`ConcurrentGunnerMode` —
  このMODにはガンナーモード自体(パイロットが2番席の視点・武器を借りて
  操作する機能)が実装されていないため、非互換としています

`AddPartRotor`のVTOL傾き自体(筐体の傾き、継続回転ではない方。`EnableVtol`/
`VtolPitch`/`VtolYaw`関連も含む)には代替機能を実装済みです。

`CameraRotationSpeed`(戦車砲塔などのカメラ回転速度制限)は変換対応・
代替機能ともに実装済みです。

`Regeneration`(2番席以降のモブの自動回復)は実装済みです。一定間隔ごとに
`LivingEntity.heal()`を直接呼び出す方式(バニラのRegenerationステータス
エフェクトは使用せず、パーティクル・アイコン表示なしで回復します)。

`DefaultFreelook`(搭乗直後からフリールック)は実装済みです。ただし、
この MOD 独自の仕様として**通常のフリールックとは挙動が逆転**しています:
`default_freelook`を有効にした機体は、既定でフリールック状態となり、
解除キーを押している間だけ通常の固定視点に戻ります。

`AddPartCamera`(常にプレイヤーの向く方向を向くパーツ)は実装済みです
(`weapon_parts`、武器と紐付かない`WeaponPart`として変換)。2番席が
占有されていればその搭乗者の向きを、そうでなければパイロットの向きを
追従します。パーツの命名規則は`$camera0`, `$camera1`, ...です。

`AddPartThrottle`(スロットルに連動して回転/移動するパーツ)は実装済みです
(`toggle_parts`の`throttle`トリガー。移動量が指定されている場合は`slide_rotate`
モード(`AddPartSlideRotLG`と同じ、回転してからスライドする方式)、それ以外は
回転のみの`rotate`モードで変換されます)。パーツの命名規則は`$throttle0`,
`$throttle1`, ...です。

`AddPartWeapon`/`AddPartTurretWeapon`/`AddPartRotWeapon`(照準連動のヨー/
ピッチ回転)は実装済みです(`weapon_parts`)。対応する武器の座席の搭乗者の
向きに合わせて、そのパーツが回転します。`AddPartRotWeapon`固有の「使用中は
ガトリングのように回転し続ける」演出、および`AddPart`/`AddChildPart`の
`part_type=2`(駐退距離、発砲時に後退してから戻る演出)も実装済みです。

`AddPartLGHatch`(ギアの折りたたみ/展開の**動作中のみ**開くハッチ、
`toggle_parts`の`landing_gear_hatch`トリガー)は実装済みです。

`AddPartWeaponBay`/`AddPartSlideWeaponBay`(対応する武器を選択中に自動で
開き、選択解除で閉じる)は実装済みです(`toggle_parts`の`weapon_bay`
トリガー)。武器選択は全プレイヤーに同期されます。

`AddPartWeaponMissile`(残弾数に連動した表示/非表示)は実装済みです
(`ammo_parts`)。同じ武器名を参照する複数のパーツを設定すると、残弾が
減るごとに1つずつ非表示になります。

`AddPartWing`/`AddPartPylon`(折りたたみ主翼・可変翼)は実装済みです
(`toggle_parts`の`wing_fold`トリガー、Nキーで切り替え)。
`VariableSweepWing`未設定/falseの場合は地上保管用の折りたたみとして扱い、
停止中のみ折りたたみ可能で、折りたたむとスロットルが0固定になり動けなく
なります。`VariableSweepWing = true`の場合は可変翼として、飛行中でもNキーで
スイープ可能で、折りたたんだ間は`max_speed`の代わりに`SweepWingSpeed`を
上限速度として使用します(実機の可変翼と同様、後退翼にするほど高速)。

---

## ビルド方法

```bash
./gradlew build
```

`build/libs/tudursvehiclemod-0.0.1.jar` が生成されます。

## バージョンについて(重要)

- Minecraft 1.21.11 は **Fabricが Yarn マッピングを更新する最後のバージョン**です
  (以降は Mojang 公式マッピングへの移行が推奨されています)。
- `gradle.properties` の `yarn_mappings` / `fabric_version` / `loader_version` は
  執筆時点の値です。ビルド前に https://fabricmc.net/develop/ で最新のビルド番号を確認してください。
