# Tudur's Vehicle Mod - 説明書

`README.md`(開発者向けアーキテクチャ資料)とは別に、機能概要・拡張パックの作り方・
MCヘリアドオンの変換方法を簡潔にまとめたものです。詳細な実装や未対応機能の一覧は
`README.md`を参照してください。

> **重要**: アドオンパックの作成にあたっては、他者が権利を有するモデル・
> データの取り扱いについて`GUIDELINES.md`を必ず確認してください。MCヘリ
> 向けに公開されている拡張パックの変換にも制限があります。

## 1. このMODでできること

- **乗り物**: ヘリコプター・車・船・潜水艦・固定翼機の5種類の動作モデルに対応。
  JSON定義 + OBJモデルを追加するだけで新しい乗り物を増やせます(コード変更不要)。
- **武装**: 機関銃・ロケット・爆弾・魚雷・誘導ミサイル(対地/対空/対戦車)に対応。
  弾数(マガジン)・熱量式(オーバーヒート)・両方併用のいずれにも対応。
- **耐久・撃破**: 機体ごとのHP・装甲係数・撃破時の演出(黒焦げ化+黒煙、搭乗者への
  ダメージ)、急角度でのブロック衝突や高速でのエンティティ衝突による大ダメージ。
- **燃料**: 機体ごとの燃料タンク・消費レート(スロットル依存、緩急カーブあり)。
  燃料切れでスロットルが0固定になります(操縦自体は可能)。
- **補給**: 搭乗中に開ける機体メニューから、弾薬補給(アイテム消費)・燃料補給
  (携行缶をセット)・機体インベントリ(設定時)を利用可能。
- **携行缶と給油装置**: 鉄インゴットで携行缶を作成、ピストン+かまど8個で給油装置を
  作成し、燃料系アイテムを燃料に変換して携行缶へ、携行缶から機体へ供給します。
  クリエイティブ専用の無限燃料缶も用意されています。
- **HUD**: MCヘリ形式のHUDスクリプト(`assets/.../hud/*.txt`)をそのまま解釈して
  描画します。体力・燃料・弾薬・熱量などの変数に対応しています。
- **空母(Carrier)**: 乗り物の種類を問わず滑走路を設定し、専用武器から艦載機を
  発艦させ、自動で哨戒/攻撃ルートを飛行、帰投・着艦させることができます。詳細は
  `README.md`の「空母(Carrier)システム」セクションを参照してください。
- **無人機・自律ドローン・CAS**: 登録スティック+専用ブロックにより、機体を無人の
  遠隔操縦(UAV)・自律AI飛行(TargetDrone)として運用できるほか、武器から支援機を
  自動出撃させる近接航空支援(CAS)にも対応しています。詳細は`README.md`の
  「無人機(UAV)・自律ドローン・近接航空支援(CAS)」セクションを参照してください。

## 2. 拡張パック(新しい乗り物)の作り方

新しい乗り物を追加するのにコードの変更は不要です。以下を用意してください。

1. **モデル**: `assets/<namespace>/models/obj/<名前>.obj`(OBJ形式、三角形/四角形/
   多角形いずれも可)
2. **テクスチャ**: `assets/<namespace>/textures/vehicle/<名前>.png`
3. **データ定義**: `data/<namespace>/vehicles/<名前>.json`
   - `entity_type`: 動作モデル(`tudursvehiclemod:car`/`helicopter`/`ship`/`submarine`/
     `aircraft`のいずれか)
   - `model`/`texture`: 上記1・2で用意したファイルの参照
   - 速度・当たり判定・座席・武装・耐久・燃料などの各種パラメータ
   - (任意)`weapons`で武装、`spawn_item`でスポーンアイテム、`hud`でHUD指定
4. `/reload`でゲーム内に反映されます。再起動不要です。

**設定できる全項目の詳細な一覧は、以下のパック製作者向けドキュメントを
参照してください**(本セクションはあくまで概要です):

- `Readme_Addon.md` — アドオンパックの配置方法・ディレクトリ構造・命名規則
- `Readme_Vehicle.md` / `Readme_Vehicle_○○.md` — 機体設定の全項目
  (共通項目 + 機体タイプ別の項目)
- `Readme_Weapon.md` / `Readme_Weapon_○○.md` — 武器設定の全項目
  (共通項目 + 武器タイプ別の項目)
- `Readme_HUD.txt` — HUDスクリプトの書式

既存の`data/tudursvehiclemod/vehicles/*.json`のサンプルもあわせて参照して
ください。

## 3. MCヘリアドオンの変換方法

既存のMCヘリ用アドオンパックを、このMODの形式へ自動変換するツールを同梱しています。
コマンドライン版とGUI版のどちらも同じ変換処理を行うため、結果は同じです。

### GUI版

```bash
python3 tools/mcheli_convert_gui.py
```

フォルダ選択ダイアログと進捗ログだけのシンプルなウィンドウが開きます。「変換元」に
MCヘリアドオンパックのフォルダ、「変換先」に出力先フォルダを指定し、「変換開始」を
押すだけです。

### コマンドライン版

```bash
python3 tools/mcheli_convert.py <MCヘリアドオンのパス> <出力先パス> [--namespace 名前空間] [--no-auto-detect]
```

- `--namespace`: 省略時は`mcheliport`
- `--no-auto-detect`: 乗り物の種類(ヘリ/航空機/車/船/潜水艦)の自動判定を無効にし、
  常に元のフォルダ名(helicopters/planes/tanks/vehicles)通りに変換します。
  自動判定は`AddRotor`/`AddCrawlerTrack`の有無や、`planes`フォルダ内での
  `Float`/`Gravity`の値から船・潜水艦・水上機を判別する仕組みですが、
  意図しない判定結果になる場合にこのオプションで無効化できます。

### 共通の仕様

- 機体設定ファイル(`.txt`)・モデル(`.obj`/`.mqo`)・テクスチャ・武器設定・
  HUDスクリプト・サウンドを読み取り、このMODの形式(JSON + アセット)へ変換します。
- 武器のダメージ・弾速・弾数などの数値は変換時に焼き込まず、変換後も
  元の武器設定ファイル(`.txt`)を直接読み込みます。バランス調整はこの`.txt`を
  編集して`/reload`するだけで反映され、再変換は不要です。
- 一部の機能は現状未対応です。詳細は`README.md`の「未対応の機能」セクションを
  参照してください。
- 変換後の出力フォルダを、通常のリソース/データパックと同様に
  `resourcepacks`または対応する場所へ配置してください。

## 4. 注意事項

- 本MOD自体にはMCヘリのアセット(モデル・テクスチャ・サウンド・機体/武器設定)は
  一切同梱していません。変換ツールはユーザー自身が別途用意したアドオンパックを
  読み込むだけです。
- 動作確認済みバージョン: Minecraft 1.21.11 / Fabric
